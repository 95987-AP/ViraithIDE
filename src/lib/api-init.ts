/**
 * Side-effect module kept for backwards compatibility.
 *
 * SECURITY: Never hardcode API keys in the repository.
 * Users should configure keys via `.env.local` (gitignored) or via the app UI.
 */
export {};
